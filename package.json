{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd v2 && npm install --no-audit --no-fund && npm run dev",
    "build": "cd v2 && npm install --no-audit --no-fund && npm run build",
    "lint": "cd v2 && npm run lint",
    "preview": "cd v2 && npm run preview",
    "typecheck": "cd v2 && npm run typecheck"
  }
}
